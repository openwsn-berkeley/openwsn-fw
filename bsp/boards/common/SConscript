import os

Import('env')

localEnv = env.Clone()

source   = ['aes_cbc_mac.c', 'aes_ccms.c', 'aes_ctr.c', 'aes_ecb.c', 'crypto_driver.c']

localEnv.Append(
        CPPPATH =  [ \
        ],
    )

common  = localEnv.Object(source=source)

Return('common')

